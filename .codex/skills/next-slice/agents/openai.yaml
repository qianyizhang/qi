interface:
  display_name: "Next slice"
  short_description: "Choose one verified next implementation slice"
  default_prompt: "Use $next-slice to verify planning state and recommend one valuable, testable slice."
